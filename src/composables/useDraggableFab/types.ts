/** Screen corner for FAB positioning. */
export type Corner = 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right'

/** Pixel coordinates for FAB placement. */
export interface FabCoords {
  readonly x: number
  readonly y: number
}
