/** FAB button size in pixels. */
export const FAB_SIZE = 56

/** Edge margin for FAB positioning. */
export const FAB_MARGIN = 16

/** Minimum movement to count as drag (not tap). */
export const DRAG_THRESHOLD = 10
